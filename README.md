# SmartCook - Интернет-магазин кухонной техники

[![Production](https://img.shields.io/badge/production-live-brightgreen)](https://kitchen-shop-l4jl.onrender.com)
[![Node.js](https://img.shields.io/badge/node-%3E%3D18-green)](https://nodejs.org)
[![PostgreSQL](https://img.shields.io/badge/postgresql-18-blue)](https://www.postgresql.org)

## 🚀 Описание

Современный интернет-магазин кухонной техники с полным функционалом e-commerce: авторизация, корзина, избранное, оформление заказов и система отзывов.

**🌐 Демо**: [kitchen-shop-l4jl.onrender.com](https://kitchen-shop-l4jl.onrender.com)

## 📋 Технологии

### Frontend
- HTML5, CSS3, JavaScript (ES6+)
- Адаптивный neon cyberpunk дизайн
- Динамическая загрузка контента
- Локальное хранилище для сессий

### Backend
- Node.js + Express.js
- RESTful API
- bcrypt для хеширования паролей
- CORS для кросс-доменных запросов

### База данных
- PostgreSQL 18
- Нормализованная структура
- Ограничения целостности (foreign keys, unique constraints)
- Каскадное удаление

### Деплой
- Render.com (Frankfurt region)
- Auto-deploy из GitHub
- Managed PostgreSQL с SSL

## 🛠️ Локальная установка

### Требования
- Node.js 18+
- PostgreSQL 18+
- npm или yarn

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/Cherkasov-Alexey/kitchen-shop.git
cd kitchen-shop
```

### 2. Установите зависимости
```bash
npm install
```

### 3. Настройте PostgreSQL

Создайте базу данных:
```bash
psql -U postgres
CREATE DATABASE kitchen_shop;
\c kitchen_shop
\i database/schema_pg.sql
\q
```

### 4. Настройте переменные окружения

Откройте [backend/src/index.js](backend/src/index.js) и обновите параметры подключения (строка ~21):
```javascript
const pgConfig = {
    host: 'localhost',
    port: 5432,
    user: 'postgres',
    database: 'kitchen_shop',
    password: 'ваш_пароль'
};
```

### 5. Запустите сервер
```bash
npm start
```

### 6. Откройте в браузере
```
http://localhost:3000
```

## ☁️ Production деплой (Render.com)

Проект настроен на автоматический деплой через GitHub.

### Текущий production
- 🌐 **URL**: https://kitchen-shop-l4jl.onrender.com
- 🗄️ **База данных**: PostgreSQL 18 (Frankfurt)
- 🔄 **Auto-deploy**: включен при push в `main`
- 🔒 **SSL**: автоматически настроен

### Быстрый деплой (с нуля)

**1. Создайте PostgreSQL на Render:**
- Dashboard → New → PostgreSQL
- Region: Frankfurt
- Name: `kitchen_shop_db`
- Скопируйте **External Database URL**

**2. Импортируйте схему:**
```bash
psql "postgresql://user:pass@host/db" -f database/schema_pg.sql
```

**3. Создайте Web Service:**
- Dashboard → New → Web Service
- Подключите GitHub репозиторий
- Настройки:
  - **Build Command**: `npm install`
  - **Start Command**: `npm start`
  - **Environment Variable**: 
    - `DATABASE_URL` = ваш External Database URL

**4. Deploy:**
Render автоматически задеплоит после сохранения настроек.

### Управление

**Просмотр БД:**
```bash
node db-render.js
```

**Логи:**
Render Dashboard → Logs

**Переменные окружения:**
Render Dashboard → Environment → Environment Variables

### ⚠️ Важно

- **Free план**: сервис засыпает после 15 мин бездействия (холодный старт ~30 сек)
- **SSL**: включен автоматически для production
- **Регион**: БД и Web Service должны быть в одном регионе

## 📁 Структура проекта

```
kitchen-shop/
├── backend/
│   └── src/
│       ├── index.js          # Express сервер + API endpoints
│       └── db-adapter.js     # PostgreSQL адаптер с SSL поддержкой
├── database/
│   └── schema_pg.sql         # Схема базы данных (8 таблиц)
├── frontend/
│   └── public/
│       ├── css/              # Стили (style.css, auth.css, profile.css)
│       ├── js/               # Модули (api-v14.js, config.js, cart.js, ...)
│       ├── images/           # Изображения товаров
│       └── *.html            # 13 HTML страниц
├── db-render.js              # Скрипт для просмотра Render БД
├── package.json              # Зависимости и скрипты
├── render.yaml               # Конфигурация Render деплоя
└── README.md
```

### Ключевые файлы

- **backend/src/db-adapter.js** - адаптер для PostgreSQL с:
  - Конвертацией `?` → `$1, $2...` плейсхолдеров
  - Автонастройкой SSL для production
  - Поддержкой DATABASE_URL строки подключения
  
- **frontend/public/js/config.js** - глобальная конфигурация:
  - Динамическое определение API_BASE_URL
  - Утилиты для форматирования дат
  
- **render.yaml** - автоматический деплой на Render
- **db-render.js** - утилита для просмотра production БД

## ✨ Функциональность

### Для пользователей
- 🔐 **Регистрация и авторизация** - безопасное хранение паролей (bcrypt)
- 🛒 **Корзина** - добавление, удаление, изменение количества
- ❤️ **Избранное** - сохранение понравившихся товаров
- 📦 **Оформление заказов** - с указанием контактных данных
- ⭐ **Система отзывов** - рейтинг от 1 до 5 звезд
- 🏷️ **Категории** - Холодильники, Кофемашины, Посудомоечные машины
- 💰 **Скидки** - специальный раздел "Товары со скидкой"
- 👤 **Профиль пользователя** - история заказов, дата регистрации
- 📱 **Адаптивный дизайн** - работает на всех устройствах

### Технические особенности
- **SPA-подобная навигация** - динамическая загрузка без перезагрузки
- **Event delegation** - оптимизация производительности
- **REST API** - чистая архитектура backend
- **Нормализованная БД** - foreign keys, unique constraints, cascades
- **Автоматическое определение окружения** - localhost vs production API
- **Московское время** - корректное отображение дат (UTC+3)

## 🗃️ База данных

### Структура (8 таблиц)
- **users** - пользователи (id, user_name, email, password_hash, created_at)
- **categories** - категории товаров
- **products** - товары с ценами и описаниями
- **cart** - корзина покупок (user_id + product_id + quantity)
- **favorites** - избранные товары
- **orders** - заказы (total, status, customer_name, phone)
- **order_items** - состав заказов (связь orders ↔ products)
- **reviews** - отзывы с рейтингом (1-5 звезд)

### Ключевые особенности
- **Foreign keys** с каскадным удалением
- **Unique constraints** (email, cart user+product, favorites user+product)
- **Timestamp** для created_at во всех таблицах
- **Decimal** для цен (точность до копеек)

### Просмотр данных

**Локальная БД:**
```bash
psql -U postgres -d kitchen_shop -c "SELECT * FROM products;"
```

**Production БД (Render):**
```bash
node db-render.js
```

Этот скрипт показывает все таблицы с корректным московским временем (UTC+3).

## 📞 API Endpoints

### Товары
- `GET /api/products` - список товаров
- `GET /api/products/:id` - товар по ID
- `GET /api/categories` - категории

### Корзина
- `GET /api/cart/:userId` - корзина пользователя
- `POST /api/cart` - добавить товар
- `PUT /api/cart` - изменить количество
- `DELETE /api/cart/:userId/:productId` - удалить товар

### Избранное
- `GET /api/favorites/:userId` - избранное пользователя
- `POST /api/favorites` - добавить в избранное
- `DELETE /api/favorites/:userId/:productId` - удалить

### Заказы
- `POST /api/orders` - создать заказ
- `GET /api/orders/:userId` - заказы пользователя

### Отзывы
- `GET /api/reviews/:productId` - отзывы товара
- `POST /api/reviews` - добавить отзыв

### Авторизация
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход

## 🎨 Страницы

### Основные
1. [index.html](frontend/public/index.html) - Главная страница
2. [catalog.html](frontend/public/catalog.html) - Каталог товаров с фильтрацией
3. [product.html](frontend/public/product.html) - Детальная страница товара с отзывами
4. [sales.html](frontend/public/sales.html) - Товары со скидкой

### Покупки
5. [cart.html](frontend/public/cart.html) - Корзина
6. [favorites.html](frontend/public/favorites.html) - Избранное
7. [checkout.html](frontend/public/checkout.html) - Оформление заказа
8. [order-success.html](frontend/public/order-success.html) - Успешное оформление

### Аккаунт
9. [register.html](frontend/public/register.html) - Регистрация
10. [login.html](frontend/public/login.html) - Вход
11. [profile.html](frontend/public/profile.html) - Профиль и история заказов

### Информация
12. [contacts.html](frontend/public/contacts.html) - Контакты
13. [help.html](frontend/public/help.html) - Помощь

## 🔧 Полезные команды

### Разработка
```bash
# Запуск сервера
npm start

# Открыть в браузере
start http://localhost:3000
```

### База данных
```bash
# Просмотр production БД (Render)
node db-render.js

# Локальная БД через psql
psql -U postgres -d kitchen_shop

# Пересоздать локальную схему
psql -U postgres -d kitchen_shop -f database/schema_pg.sql

# Экспорт данных
pg_dump -U postgres kitchen_shop > backup.sql
```

### Git & Deploy
```bash
# Коммит и автодеплой
git add .
git commit -m "Update"
git push  # автоматически задеплоится на Render
```

## 🐛 Troubleshooting

### Проблема: "Cannot connect to database"
**Решение**: Проверьте DATABASE_URL в environment variables на Render

### Проблема: "Application failed to respond"
**Решение**: Проверьте логи в Render Dashboard → Logs

### Проблема: Долгая загрузка первого запроса
**Причина**: Free план Render - сервис засыпает после 15 мин  
**Решение**: Подождите ~30 сек (холодный старт)

### Проблема: Товары не загружаются
**Решение**: 
1. Проверьте браузерную консоль (F12)
2. Убедитесь что схема БД применена
3. Проверьте `/api/products` endpoint

### Проблема: Отзывы не сохраняются
**Решение**: Убедитесь что пользователь авторизован (localStorage → currentUser)

## 📦 Зависимости

```json
{
  "express": "^4.18.2",
  "pg": "^8.11.3",
  "bcrypt": "^5.1.1",
  "cors": "^2.8.5"
}
```

- **express** - веб-фреймворк
- **pg** - PostgreSQL клиент для Node.js
- **bcrypt** - хеширование паролей
- **cors** - CORS middleware

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Cherkasov Alexey

## 🔗 Ссылки

- **Production**: https://kitchen-shop-l4jl.onrender.com
- **Repository**: https://github.com/Cherkasov-Alexey/kitchen-shop
- **Database**: PostgreSQL 18 на Render (Frankfurt)