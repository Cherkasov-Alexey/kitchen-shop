# SmartCook - Интернет-магазин кухонной техники

## 🚀 Описание

Полнофункциональный интернет-магазин с базой данных PostgreSQL, системой авторизации, корзиной, избранным и оформлением заказов.

## 📋 Технологии

- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Backend**: Node.js + Express.js
- **База данных**: PostgreSQL 18
- **Безопасность**: bcrypt для хеширования паролей
- **Дизайн**: Адаптивный neon cyberpunk стиль

## 🛠️ Установка и запуск

### 1. Установите зависимости
```bash
npm install
```

### 2. Настройте PostgreSQL

Создайте базу данных и запустите схему:
```bash
psql -U postgres
CREATE DATABASE kitchen_shop;
\c kitchen_shop
\i database/schema_pg.sql
\q
```

### 3. Настройте подключение к БД

Откройте `backend/src/index.js` и обновите параметры подключения (строка ~19):
```javascript
const pgConfig = {
    host: 'localhost',
    port: 5432,
    user: 'postgres',
    database: 'kitchen_shop',
    password: 'ваш_пароль'
};
```

### 4. Запустите сервер
```bash
npm start
```

### 5. Откройте в браузере
```
http://localhost:3000
```

## ☁️ Деплой на Render

Краткая инструкция для деплоя на Render.com:

1. Создайте аккаунт на https://render.com и подключите репозиторий (GitHub/GitLab).
2. В корне репозитория есть `render.yaml` — можно использовать его для автоматического создания сервиса и базы данных.
3. В Render откроется managed Postgres. В разделе `Databases` получите параметры подключения (или `DATABASE_URL`).
4. В настройках Web Service в Render добавьте переменные окружения, которые использует приложение:

    - `DB_HOST` — хост базы
    - `DB_PORT` — порт (обычно 5432)
    - `DB_USER` — пользователь
    - `DB_PASSWORD` — пароль
    - `DB_NAME` — имя базы (kitchen_shop)

    (Если хотите, можно изменить `backend/src/index.js`, чтобы использовать `process.env.DATABASE_URL` напрямую.)

5. Запустите миграцию/схему для создания таблиц. В Render можно выполнить это через подключение к базе (`psql`) или через Shell сервиса:

```bash
# получить DATABASE_URL из Render (или сконфигурировать переменные как выше)
# затем выполнить (локально или в Shell на Render):
psql "${DATABASE_URL}" -f database/schema_pg.sql
```

6. После успешного применения схемы открывайте сайт по URL, который присвоит Render.

Полезно: `render.yaml` уже содержит пример сервиса и базы; исправьте `region`/`plan` при необходимости.

## 📁 Структура проекта

```
kitchen-shop/
├── .vscode/                  # VSCode settings (optional)
├── backend/
│   └── src/
│       ├── index.js         # Express сервер + API endpoints
│       └── db-adapter.js    # PostgreSQL адаптер
├── database/
│   ├── schema_pg.sql        # Схема базы данных PostgreSQL
│   └── view-all-tables.js   # Скрипт для просмотра данных
├── frontend/
│   └── public/
│       ├── css/             # Стили (auth.css, profile.css, style.css)
│       ├── js/              # JavaScript модули (api-v14.js, main.js, ...)
│       ├── images/          # Изображения товаров
│       └── *.html           # HTML страницы (index, catalog, product...)
├── node_modules/
├── package.json
├── package-lock.json
└── README.md
```

## ✨ Основные функции

### Для пользователей:
- 🔐 Регистрация и авторизация с bcrypt
- 🛒 Корзина с изменением количества товаров
- ❤️ Избранное
- 📦 Оформление заказов
- ⭐ Система отзывов с рейтингом
- 🏷️ Категории и товары со скидками
- 📱 Адаптивный дизайн

### Технические особенности:
- Динамическая загрузка контента без перезагрузки страницы
- Event delegation для оптимизации производительности
- Безопасное хранение паролей (bcrypt)
- PostgreSQL с триггерами и ограничениями
- RESTful API

## 🗃️ База данных

### Основные таблицы:
- `users` - пользователи
- `categories` - категории товаров
- `products` - товары
- `cart` - корзина
- `favorites` - избранное
- `orders` - заказы
- `order_items` - товары в заказах
- `reviews` - отзывы

### Просмотр данных:
```bash
node database/view-all-tables.js
```

## 📞 API Endpoints

### Товары
- `GET /api/products` - список товаров
- `GET /api/products/:id` - товар по ID
- `GET /api/categories` - категории

### Корзина
- `GET /api/cart/:userId` - корзина пользователя
- `POST /api/cart` - добавить товар
- `PUT /api/cart` - изменить количество
- `DELETE /api/cart/:userId/:productId` - удалить товар

### Избранное
- `GET /api/favorites/:userId` - избранное пользователя
- `POST /api/favorites` - добавить в избранное
- `DELETE /api/favorites/:userId/:productId` - удалить

### Заказы
- `POST /api/orders` - создать заказ
- `GET /api/orders/:userId` - заказы пользователя

### Отзывы
- `GET /api/reviews/:productId` - отзывы товара
- `POST /api/reviews` - добавить отзыв

### Авторизация
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход

## 🎨 Страницы

1. **index.html** - Главная страница
2. **catalog.html** - Каталог товаров
3. **product.html** - Страница товара
4. **cart.html** - Корзина
5. **favorites.html** - Избранное
6. **checkout.html** - Оформление заказа
7. **order-success.html** - Успешный заказ
8. **sales.html** - Товары со скидкой
9. **register.html** - Регистрация
10. **login.html** - Вход
11. **profile.html** - Профиль пользователя
12. **contacts.html** - Контакты
13. **help.html** - Помощь

## 🔧 Полезные команды

```bash
# Запуск сервера
npm start

# Просмотр данных в БД
node database/view-all-tables.js

# Просмотр через psql
psql -U postgres -d kitchen_shop -c "SELECT * FROM products;"
```

## 📝 Примечания

- Для работы требуется PostgreSQL 18+
- Node.js версии 14+
- Порт по умолчанию: 3000
- База данных: kitchen_shop