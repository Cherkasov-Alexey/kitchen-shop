# ✅ Чеклист для развертывания на Render

## Подготовка (выполнено)
- [x] SSL поддержка добавлена в db-adapter.js
- [x] Health check endpoint улучшен
- [x] package.json содержит engines.node >= 18.0.0
- [x] render.yaml создан
- [x] .env.example создан
- [x] README.md обновлен с инструкциями
- [x] .gitignore правильно настроен

## Шаги для деплоя

### 1. Загрузка кода в GitHub
```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Prepare for Render deployment"

# Добавьте remote репозиторий
git remote add origin <ваш-github-repo-url>

# Загрузите код
git push -u origin main
```

### 2. Создание PostgreSQL базы данных на Render

1. Откройте https://dashboard.render.com/
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните:
   - **Name**: `kitchen-shop-db`
   - **Database**: `kitchen_shop`
   - **User**: (автоматически)
   - **Region**: **Frankfurt** (ближе к Европе)
   - **Plan**: **Free**
4. Нажмите **"Create Database"**
5. Дождитесь создания (1-2 минуты)
6. Скопируйте **"External Database URL"** (формат: postgresql://user:pass@host:port/db)

### 3. Импорт схемы в базу данных Render

```bash
# Используйте скопированный DATABASE_URL
psql "<ваш_DATABASE_URL>" -f database/schema_pg.sql
```

Если psql не доступен, используйте альтернативный способ:
- В Render Dashboard откройте вашу БД
- Нажмите **"Connect"** → **"External Connection"**
- Используйте любой PostgreSQL клиент для подключения
- Выполните содержимое файла database/schema_pg.sql

### 4. Создание Web Service

1. В Render Dashboard: **"New +"** → **"Web Service"**
2. Подключите GitHub репозиторий
3. Выберите репозиторий `kitchen-shop`
4. Заполните настройки:

**Basic Settings:**
- **Name**: `kitchen-shop` (или ваше имя)
- **Region**: **Frankfurt** (тот же что и БД!)
- **Branch**: `main`
- **Runtime**: **Node**

**Build & Deploy:**
- **Build Command**: `npm install`
- **Start Command**: `npm start`

5. Раскройте **"Advanced"**

6. Добавьте **Environment Variables**:
   
   | Key | Value |
   |-----|-------|
   | `NODE_ENV` | `production` |
   | `DATABASE_URL` | `<ваш External Database URL из шага 2>` |

7. Нажмите **"Create Web Service"**

### 5. Ожидание деплоя

- Следите за процессом в разделе **"Logs"**
- Деплой занимает 2-5 минут
- Дождитесь сообщения "Live"

### 6. Проверка работы

1. Откройте ваш URL (например: https://kitchen-shop.onrender.com)

2. Проверьте health endpoint:
   ```
   https://kitchen-shop.onrender.com/health
   ```
   Должен вернуть:
   ```json
   {
     "status": "healthy",
     "database": "connected",
     "environment": "production",
     "timestamp": "..."
   }
   ```

3. Проверьте главную страницу:
   ```
   https://kitchen-shop.onrender.com/
   ```

4. Проверьте API:
   ```
   https://kitchen-shop.onrender.com/api/products
   https://kitchen-shop.onrender.com/api/categories
   ```

## Возможные проблемы и решения

### Проблема 1: "Application failed to respond"
**Решение:**
- Проверьте логи в Render Dashboard
- Убедитесь что DATABASE_URL правильный
- Проверьте что регионы БД и Web Service совпадают

### Проблема 2: "Database connection error"
**Решение:**
- Проверьте что DATABASE_URL скопирован полностью
- Убедитесь что схема импортирована в БД Render
- Проверьте что SSL настроен (уже сделано в коде)

### Проблема 3: "Build failed"
**Решение:**
- Проверьте что package.json корректный
- Убедитесь что все зависимости указаны
- Проверьте Node.js версию в engines

### Проблема 4: Медленный первый запрос
**Это нормально** для Free плана:
- Сервис засыпает после 15 минут неактивности
- Первый запрос "будит" сервер (может занять 30-60 секунд)
- Последующие запросы работают быстро

## После успешного деплоя

### Настройка автодеплоя (уже включен)
- При каждом push в main ветку автоматически запускается деплой

### Мониторинг
- Dashboard → Metrics: CPU, RAM, запросы
- Dashboard → Logs: логи приложения в реальном времени

### Обновление приложения
```bash
# Внесите изменения
git add .
git commit -m "Update feature"
git push origin main

# Render автоматически задеплоит изменения
```

## Полезные команды

```bash
# Просмотр БД на Render
psql "<DATABASE_URL>" -c "SELECT * FROM products;"

# Бэкап БД
pg_dump "<DATABASE_URL>" > backup.sql

# Восстановление БД
psql "<DATABASE_URL>" < backup.sql
```

## Дополнительные настройки (опционально)

### Настройка кастомного домена
1. Dashboard → Settings → Custom Domain
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкции

### Апгрейд плана
Free план ограничения:
- 750 часов работы в месяц (засыпание после неактивности)
- Медленный первый запрос после пробуждения

Starter план ($7/мес):
- Без засыпания
- Быстрая работа 24/7
- Больше ресурсов

## Готово! 🎉

Ваш сайт теперь доступен онлайн на Render!
